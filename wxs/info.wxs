  //JS代码：ES5
  var message = "Hello World!";
  var name = "Berry"

  var sum = function(num1, num2) {
    return num1 + num2;
  }

  //模块化导出
  module.exports = {
    message: message,
    name: name,
    sum: sum
  }